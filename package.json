{
  "name": "algorithm-questions",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf ./dist && tsc --target es2019 --outDir dist",
    "build-watch": "rm -rf ./dist && tsc --target es2019 --outDir dist --watch --preserveWatchOutput",
    "test": "jest ./dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@jest/globals": "29.5.0",
    "jest": "29.5.0",
    "typescript": "5.0.4"
  },
  "jest": {
    "testMatch": [
      "**/dist/__tests__/*.test.js"
    ]
  }
}
